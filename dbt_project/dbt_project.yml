name: 'tenerife_tourism_dbt'
version: '1.0.0'
config-version: 2
profile: 'tenerife_tourism_dbt'
model-paths: ["models"]
models:
  tenerife_tourism_dbt:
    silver:
      +materialized: table
    gold:
      +materialized: table
      +schema: gold
      silver_sentiment_results:
        +enabled: true
      silver_aspect_results:
        +enabled: true