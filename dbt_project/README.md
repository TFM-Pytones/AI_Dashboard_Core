# dbt_project/

Issue #4 — Orquestación Inicial dbt.

Transformaciones en base de datos (modelos dbt) conectadas a PostgreSQL/Neon. Incluye `sources.yml` definiendo las tablas crudas cargadas por `ingestion/`.
