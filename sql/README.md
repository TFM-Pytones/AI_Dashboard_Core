# sql/

Diseño del Data Warehouse espacial en PostgreSQL/PostGIS (sección 3.1 del índice del TFM).

Contenido esperado: DDL de esquemas (`raw_data`, `processed_data`), definición de tablas, y los índices GiST para las coordenadas estandarizadas a EPSG:32628 (issue #6).
